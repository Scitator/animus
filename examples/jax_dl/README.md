## Tl;dr
```bash
# jax https://github.com/google/jax#installation

pip install animus numpy torch torchvision tqdm
python jax_run.py

pip install animus flax numpy optax torch torchvision tqdm
python flax_run.py

pip install animus git+https://github.com/deepmind/dm-haiku numpy optax torch torchvision tqdm
python haiku_run.py
```
